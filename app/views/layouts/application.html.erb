<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>

    <ul>
      <li><a href="/my_collections">My Collections</a></li>
      <li><a href="/collections">All Collections</a></li>
      <li><a href="/positions">All Positions</a></li>
      
      <% if session.fetch(:user_id) == nil %>
        <li><a href="/user_sign_in">User Sign-in</a></li>
        <li><a href="/user_sign_up">User Sign-up</a></li>
      <% else%>
        <li><a href="/user_sign_out">Sign out</a></li>
        <li>Signed in as <%=@current_user.email%></li>
        <li><%= session.fetch(:user_id) %></li>
      <% end %>
    </ul>

    <%= yield %>
  </body>
</html>
